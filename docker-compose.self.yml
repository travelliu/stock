# Build override: build stockd from the current checkout instead of pulling an image.
# Merged on top of docker-compose.yml via: make selfhost-build

services:
  stockd:
    image: stockd:dev
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-dev}
        COMMIT: ${COMMIT:-unknown}
